# Python Streamer

The main streaming script `app35_cam_sole.py` should be symlinked or copied from the ForgeOn backend:

```bash
ln -s /path/to/forgeon/backend/app/app35_cam_sole.py ./app35_cam_sole.py
```

For packaged builds, the script is bundled as an extraResource.
